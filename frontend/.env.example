# Copia este archivo como .env y completa los valores reales

# WhatsApp del administrador (mismo valor que ADMIN_WHATSAPP en el backend)
VITE_ADMIN_WHATSAPP=591XXXXXXXXX

# Clave pública VAPID (la misma que VAPID_PUBLIC_KEY en el backend .env)
VITE_VAPID_PUBLIC_KEY=tu_clave_publica_vapid_aqui
